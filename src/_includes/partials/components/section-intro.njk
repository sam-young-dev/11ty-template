  <div class="[ section-intro ] [ flow text-center ]">
    <div class="section-intro__heading">{{ introHeading }}</div>
    {% if includeDate %}
    <p class="section-intro__date">{{ page.date | shortDateFilter(false) }}</p>
    {% endif %}
  </div>